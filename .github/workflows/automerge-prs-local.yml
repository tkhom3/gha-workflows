name: Automerge PRs

on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: tkhom3/gha-workflows/.github/workflows/automerge-prs.yml@68f269256fbed22648f23ce579ddf356b95ef0f6 # v1.0.21
    secrets:
      token: ${{ secrets.GHA_PAT }}
