name: Automerge PRs

on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: tkhom3/gha-workflows/.github/workflows/automerge-prs.yml@c599efd9d16a94f8e62b85415a480a48a3f6755c # v1.0.15
    secrets:
      token: ${{ secrets.GHA_PAT }}
