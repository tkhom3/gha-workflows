name: Automerge PRs

on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: tkhom3/gha-workflows/.github/workflows/automerge-prs.yml@01efdd07d084c97e79ae8ebda78d84d9e15b0cb7 # v1.0.17
    secrets:
      token: ${{ secrets.GHA_PAT }}
