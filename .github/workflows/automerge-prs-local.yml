name: Automerge PRs

on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: tkhom3/gha-workflows/.github/workflows/automerge-prs.yml@64fcfa07e0917b1cc082567e6096ce3eccd2db47 # v1.0.9
    secrets:
      token: ${{ secrets.GHA_PAT }}
