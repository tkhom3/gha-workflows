name: Automerge PRs

on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: tkhom3/gha-workflows/.github/workflows/automerge-prs.yml@fdab52398496c12719b13bdc353d8080d6cf20ec # v1.0.10
    secrets:
      token: ${{ secrets.GHA_PAT }}
